#version: '3.8'

services:
  postgres:
    container_name: restful_code_leap_db
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=restful_code_leap_db
